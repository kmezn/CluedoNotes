<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CluedoNotes.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CluedoNotes.Pages">

    
    
    
    <!--<ShellContent
        Title="Home"
        ContentTemplate="{DataTemplate local:MainPage}"
        Route="MainPage" />-->

    <FlyoutItem Title="SetUp" >
        <ShellContent Title="Players" ContentTemplate="{DataTemplate local:SetUpPlayers}"/>
        <ShellContent Title="Rooms" ContentTemplate="{DataTemplate local:SetUpRooms}"/>
        <ShellContent Title="Suspects" ContentTemplate="{DataTemplate local:SetUpSuspects}"/>
        <ShellContent Title="Weapons" ContentTemplate="{DataTemplate local:SetUpWeapons}"/>


    </FlyoutItem>
    <FlyoutItem Title="Gameplay">
        <ShellContent Title="Players" ContentTemplate="{DataTemplate local:PlayersPage}">

        </ShellContent>

        <ShellContent Title="Rooms" ContentTemplate="{DataTemplate local:RoomsPage}">

        </ShellContent>
    </FlyoutItem>
            






</Shell>
